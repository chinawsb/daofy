mcp>=0.9.0
pydantic>=2.0.0
beautifulsoup4>=4.12.0
html2text>=2024.2.25
lxml>=4.9.0
requests>=2.31.0
python-docx>=0.8.11
# 可选依赖: 文件变更监听 —— 自动增量更新项目知识库
# pip install daofy-for-delphi[watcher]
# 或: pip install watchdog>=6.0.0
# 可选依赖: 语义搜索(embedding) —— 安装后显著提升搜索质量
# pip install daofy-for-delphi[embedding]
# 或: pip install sentence-transformers numpy
# 可选依赖: OCR 图像文字识别 —— PP-OCRv6 (纯 ONNX Runtime)
# pip install daofy-for-delphi[ocr]
# 或: pip install onnxruntime opencv-python-headless numpy pyclipper shapely
# Intel CPU 加速: pip install daofy-for-delphi[ocr-openvino]
# 或: pip install onnxruntime-openvino
chardet>=7.0
